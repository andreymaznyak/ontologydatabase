{% extends '::base.bs.html.twig' %}

{% block title -%}Ontology list{% endblock %}
{% block body_heading -%}<div class="panel-heading">Ontology list</div>{% endblock %}

{% block body -%}
    <table class="records_list table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Title</th>
                <th>Lft</th>
                <th>Rgt</th>
                <th>Root</th>
                <th>Lvl</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('list_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.lft }}</td>
                <td>{{ entity.rgt }}</td>
                <td>{{ entity.root }}</td>
                <td>{{ entity.lvl }}</td>
                <td>
                        <a class="btn btn-default" rel="tooltip" title="show" href="{{ path('list_show', { 'id': entity.id }) }}">show</a>
                        <a class="btn btn-default" rel="tooltip" title="edit" href="{{ path('list_edit', { 'id': entity.id }) }}">edit</a>                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

{% block body_footer -%}
	<div class="panel-footer">
		<a class="btn btn-default" rel="tooltip" title="Create a new entry" href="{{ path('list_new') }}">Create a new entry</a>
	</div>
{% endblock %}
